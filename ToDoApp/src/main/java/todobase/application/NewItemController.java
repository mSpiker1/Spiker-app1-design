package todobase.application;

public class NewItemController {
    //initialize all fxml objects

    //if save item button is clicked
        //grab the entries from the text field and date picker as strings

        //verify the description string using the verification method in the Item class

        //verify the date string using the verification method in the Item class

        //create a boolean "complete" with the value of the checkbox selection as its true/false

        //create a new item with these two strings and boolean as its description, due date, and completion value

        //add the item to the currently selected list
}
