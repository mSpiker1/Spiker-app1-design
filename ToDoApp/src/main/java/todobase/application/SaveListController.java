/*
 *  UCF COP3330 Fall 2021 Application Assignment 1 Solution
 *  Copyright 2021 Matthew Spiker
 */

package todobase.application;

public class SaveListController {
    //initialize all fxml objects

    //if save all lists button is clicked
        //create an array of strings and add the string "-1" as [0]

        //call the save method from the SaveLists class with the string array as the parameter

    //if save specified lists button is clicked
        //create an array of strings

        //set the array of strings to the string from the text area, split by each comma + space

        //call the save method from the SaveLists class with the string array as the parameter
}
